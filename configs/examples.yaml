# Example configuration snippets

  name: example-service

  replicas: 2

  resources:

    cpu: '200m'

  env:

    - name: APP_ENV

      value: 'development'

# keep configuration small and readable

service:

  replicas: 2

  resources:

    cpu: '200m'

